{
  "name": "heddle",
  "version": "0.0.1",
  "private": true,
  "description": "Cross-provider agent orchestration for subscription coding CLIs — routing, skill packs, inter-agent comms, and a visibility dashboard.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit -p tsconfig.test.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "yaml": "^2.5.0",
    "zod": "^3.23.0"
  },
  "bin": {
    "heddle": "dist/cli.js",
    "heddle-mcp": "dist/mcp-server.js"
  }
}
